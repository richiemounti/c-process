# Usage

# for running basic c files use the command below(assuming you have gcc installed in your system)

`gcc -o <filename> <filename.c>`

# for the threads program run the following program

`gcc -o <filename> <filename.c> -lpthread`